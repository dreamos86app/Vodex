/** @deprecated Import from user-build-copy-sanitizer — kept for backward compatibility. */
export {
  BUILD_PAUSED_HEADLINE,
  BUILD_NEEDS_ANOTHER_PASS,
  BUILD_INCOMPLETE_NO_BROKEN_PREVIEW,
  PREVIEW_NOT_AVAILABLE_YET,
  CONTINUE_GENERATION_LABEL,
  sanitizeUserBuildChatText,
  userBlockedBuildSummary,
  userContinuationProgressLine,
  isTechnicalBuildDebugText,
  containsUserFacingBuildDebug,
} from "@/lib/build/user-build-copy-sanitizer";
