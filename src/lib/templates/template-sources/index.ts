/**
 * Prebuilt template workspace files — duplicated into user projects via duplicateTemplateToProject().
 */
export { getTemplateSourceFiles } from "@/lib/templates/template-source-files";
export type { TemplateSourceFile } from "@/lib/templates/template-source-types";
