import { PublicStatusPage } from "@/components/status/public-status-page";

export const metadata = {
  title: "Vodex Status",
  description: "Vodex platform and service status",
};

export default function StatusPage() {
  return <PublicStatusPage />;
}
