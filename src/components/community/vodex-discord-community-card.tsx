"use client";

import { PremiumDiscordCard } from "@/components/ui/premium-discord-card";

export function VodexDiscordCommunityCard({ className }: { className?: string }) {
  return <PremiumDiscordCard className={className} variant="hero" />;
}
